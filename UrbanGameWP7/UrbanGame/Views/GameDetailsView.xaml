<phone:PhoneApplicationPage 
    x:Class="UrbanGame.Views.GameDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Panorama>
            <controls:Panorama.Background>
                <ImageBrush ImageSource="/Images/GameviewPanorama.png" />
            </controls:Panorama.Background>
            <controls:Panorama.Title>
                <StackPanel Orientation="Vertical">
                    <Grid Background="Transparent" Margin="0,100,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Path=Game.Name}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" FontSize="60"/>

                        <TextBlock Text="{Binding Path=AppResources.PoweredBy, Source={StaticResource LocalizedStrings}}" Grid.Row="1" Grid.Column="0"  FontSize="{StaticResource PhoneFontSizeMedium}" Margin="120,0,10,0"/>
                        <TextBlock Text="{Binding Path=AppResources.Games, Source={StaticResource LocalizedStrings}}" Grid.Row="1" Grid.RowSpan="2" Grid.Column="2" FontSize="{StaticResource PhoneFontSizeLarge}" FontWeight="Bold" Margin="-20,0,0,0"/>
                        <TextBlock Text="{Binding Path=AppResources.Urban, Source={StaticResource LocalizedStrings}}" Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorGreen}" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </controls:Panorama.Title>

            <!--Panorama item one-->
            <controls:PanoramaItem x:Name="Description" Header="{Binding Path=AppResources.Description, Source={StaticResource LocalizedStrings}}" Foreground="{StaticResource FontColorGreen}">
                <ScrollViewer>
                    <Grid Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.Resources>
                            <Style TargetType="StackPanel">
                                <Setter Property="Margin" Value="5,0,5,5" />
                            </Style>
                        </Grid.Resources>
                        <Grid Grid.Row="0" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Image Width="130" Height="130" Margin="0,10,24,0" Source="{Binding Path=Game.GameLogoFullUrl}" VerticalAlignment="Top" />

                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                <TextBlock Text="{Binding Path=Game.Name}" FontSize="{StaticResource PhoneFontSizeLarge}" FontWeight="Bold" Foreground="{StaticResource FontColorWhite}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,5,5">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                        </Style>
                                    </StackPanel.Resources>
                                    <TextBlock Text="{Binding Path=AppResources.ByOperator, Source={StaticResource LocalizedStrings}}" 
                                           Margin="0,0,10,0" FontSize="{StaticResource PhoneFontSizeNormal}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Path=Game.OperatorName}" FontSize="{StaticResource PhoneFontSizeNormal}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Game.Localization}" FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource FontColorWhite}"/>
                            </StackPanel>
                        </Grid>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Game.Points}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                            <TextBlock Text="{Binding Path=AppResources.Points, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Game.GameEnd, Converter={StaticResource GameEndConverter}}" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Grid.Row="3" Text="{Binding Game.Description}" Margin="5,10,0,5" Foreground="{StaticResource FontColorWhite}"/>
                        <StackPanel Grid.Row="4" Orientation="Horizontal" Visibility="{Binding ShowsMore, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Path=AppResources.GameType, Source={StaticResource LocalizedStrings}}" Margin="5,0,10,0" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Path=Game.GameType, Converter={StaticResource GameTypeConverter}}" />
                        </StackPanel>
                        <StackPanel Grid.Row="5" Orientation="Horizontal" Visibility="{Binding ShowsMore, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Path=AppResources.PlayersUPCase, Source={StaticResource LocalizedStrings}}" Margin="5,0,10,0" FontWeight="Bold" />
                            <TextBlock Text="{Binding Path=Game.NumberOfPlayers}" />
                            <TextBlock Text="{Binding Path=Game.NumberOfSlots, Converter={StaticResource IntToSlashIntConverter}}" />
                        </StackPanel>
                        <StackPanel Grid.Row="6" Orientation="Horizontal" Visibility="{Binding ShowsMore, Converter={StaticResource BoolToVisibilityConverter}}" >
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Path=AppResources.Duration, Source={StaticResource LocalizedStrings}}" Margin="5,0,10,0" FontWeight="Bold" />
                            <Grid Background="Transparent">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Margin="0,0,0,0" Orientation="Horizontal">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource FontColorWhite}" />
                                        </Style>
                                    </StackPanel.Resources>
                                    <TextBlock Text="{Binding Path=Game.GameStart, Converter={StaticResource DurationTimeConverter}, ConverterParameter=start}" />
                                    <TextBlock Text="-"/>
                                </StackPanel>
                                <TextBlock Grid.Row="1" Text="{Binding Path=Game.GameEnd, Converter={StaticResource DurationTimeConverter}}" Foreground="{StaticResource FontColorWhite}"/>
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Row="7" Orientation="Horizontal" Visibility="{Binding ShowsMore, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="Foreground" Value="White" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="{Binding Path=AppResources.Difficulty, Source={StaticResource LocalizedStrings}}" Margin="5,0,10,0" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Path=Game.Difficulty, Converter={StaticResource DifficultyConverter}}" />
                        </StackPanel>
                        <TextBlock Grid.Row="8" cal:Message.Attach="[Event Tap]=[Action ShowLessDescription()]" Visibility="{Binding ShowsMore, Converter={StaticResource BoolToVisibilityConverter}}" Margin="5,0,0,0">
                        <Run TextDecorations="Underline" Foreground="{StaticResource FontColorGreen}">show less</Run>
                        </TextBlock>
                        <TextBlock Grid.Row="8" cal:Message.Attach="[Event Tap]=[Action ShowMoreDescription()]" Visibility="{Binding ShowsMore, Converter={StaticResource OpositeBoolToVisibilityConverter}}" Margin="5,0,0,0">
                        <Run TextDecorations="Underline" Foreground="{StaticResource FontColorGreen}">show more</Run>
                        </TextBlock>
                    </Grid>
                </ScrollViewer>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <controls:PanoramaItem x:Name="ActiveAndInactiveTasks" Header="{Binding Path=AppResources.Tasks, Source={StaticResource LocalizedStrings}}" Foreground="{StaticResource FontColorGreen}">
                <ScrollViewer>
                    <Grid Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Vertical">
                            <TextBlock Text="{Binding Path=AppResources.Active, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}" Margin="5,0,0,0" />
                            <ListBox ItemsSource="{Binding ActiveTasks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ItemStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="Transparent" cal:Message.Attach="[Event Tap]=[Action ShowTask($dataContext)]" Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding Picture}" Width="100" Height="100" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                                <StackPanel.Resources>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Margin" Value="0,0,5,5" />
                                                        <Setter Property="Foreground" Value="{StaticResource FontColorGreen}" />
                                                    </Style>
                                                </StackPanel.Resources>
                                                <TextBlock Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorWhite}"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding IsRepeatable, Converter={StaticResource IsRepeatableConverter}}" />
                                                    <TextBlock Text="|" />
                                                    <TextBlock Text="{Binding UserPoints, TargetNullValue=?}" />
                                                    <TextBlock Text="{Binding MaxPoints, Converter={StaticResource IntToSlashIntConverter}}" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding EndDate, Converter={StaticResource GameEndConverter}}" Foreground="{StaticResource FontColorWhite}"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <TextBlock Text="{Binding Path=AppResources.BonusTask, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}" Margin="5,10,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Vertical">
                            <TextBlock Text="{Binding Path=AppResources.Inactive, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}" Margin="5,0,0,5" />
                            <ListBox ItemsSource="{Binding InactiveTasks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ItemStyle}" >
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="Transparent" cal:Message.Attach="[Event Tap]=[Action ShowTask($dataContext)]" Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding Picture}" Width="100" Height="100" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorWhite}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </controls:PanoramaItem>

            <controls:PanoramaItem x:Name="AccomplishedAndCancelledTasks" Foreground="{StaticResource FontColorGreen}">
                <ScrollViewer Margin="0,95,0,0">
                    <Grid Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Vertical">
                            <TextBlock Text="{Binding Path=AppResources.Accomplished, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                            <ListBox ItemsSource="{Binding AccomplishedTasks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ItemStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="Transparent" cal:Message.Attach="[Event Tap]=[Action ShowTask($dataContext)]" Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding Picture}" Width="100" Height="100" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                                <TextBlock Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorWhite}"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding UserPoints}" Foreground="{StaticResource FontColorGreen}"/>
                                                    <TextBlock Text="{Binding MaxPoints, Converter={StaticResource IntToSlashIntConverter}}" Foreground="{StaticResource FontColorGreen}" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding Path=AppResources.PointsShort, Source={StaticResource LocalizedStrings}}" Foreground="{StaticResource FontColorGreen}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="5,0,5,0">
                            <TextBlock Text="{Binding Path=AppResources.Cancelled, Source={StaticResource LocalizedStrings}}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                            <ListBox ItemsSource="{Binding CancelledTasks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ItemStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="Transparent" cal:Message.Attach="[Event Tap]=[Action ShowTask($dataContext)]" Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding Picture}" Width="100" Height="100" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorWhite}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </controls:PanoramaItem>

            <controls:PanoramaItem x:Name="Alerts" Header="{Binding Path=AppResources.Alerts, Source={StaticResource LocalizedStrings}}" Foreground="{StaticResource FontColorGreen}">
                <ListBox ItemsSource="{Binding GameAlerts}" ItemContainerStyle="{StaticResource ItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="0,5,0,0">
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Margin" Value="5,0,0,5" />
                                    </Style>
                                </StackPanel.Resources>
                                <TextBlock Text="Operator" Foreground="{StaticResource FontColorWhite}" FontSize="{StaticResource PhoneFontSizeLarge}" />
                                <TextBlock Text="{Binding Topic}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource FontColorWhite}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PanoramaItem>

            <controls:PanoramaItem x:Name="HighScores" Header="{Binding Path=AppResources.HighScores, Source={StaticResource LocalizedStrings}}" Foreground="{StaticResource FontColorGreen}">
                <ListBox ItemsSource="{Binding GameHighScores}" ItemContainerStyle="{StaticResource ItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Polygon Grid.Row="0" Points="0,0 80,0 80,50 0,80">
                                        <Polygon.Fill>
                                            <SolidColorBrush Color="Green" />
                                        </Polygon.Fill>
                                    </Polygon>
                                    <TextBlock Grid.Column="0" Text="{Binding Position}" Foreground="{StaticResource FontColorWhite}" FontSize="{StaticResource PhoneFontSizeLarge}" Margin="5,0,0,0"/>
                                </Grid>
                                <Grid Grid.Column="1" Margin="5,0,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="{Binding Entity.UserLogin}" FontSize="{StaticResource PhoneFontSizeLarge}" Foreground="{StaticResource FontColorWhite}"/>
                                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Entity.Points}" FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource FontColorWhite}" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding AppResouces.Points}" FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource FontColorWhite}"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>